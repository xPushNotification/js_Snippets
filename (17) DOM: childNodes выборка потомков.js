
//----------
// выборка потомков:
//----------

// все выборки возвращают не массивы, а коллекции

for(let i=0; i< document.body.childNodes.length; i++){}
if( elem.childNodes[0] === elem.firstChild ){} // true
if( elem.childNodes[elem.childNodes.length-1] == elem.lastChild ){} // true

