
//----------
// управление атрибутами:
//----------
// <img src="img_girl.jpg" width="500" height="600"> --> src, width, height

t = elem.hasAttribute("name");
t = elem.getAttribute("name");

elem.setAttribute("name", value);
elem.removeAttribute("name");

t = elem.attributes;

let inpit = document.querySelector("input");
input.setAttribute("id","firstId");
input.id = "newId";

t = input.checked;  // true/false

