
//----------
// итерация:
//----------

for(let i = 0; i< arr.length; i++){}
for(let value of arr){}     // значения
for(let key in arr){}       // ключи

for(let key of Object.keys(arr)) {}
for(let value of Object.values(arr)) {}