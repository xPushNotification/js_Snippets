
//----------
// ... развернуть итератив
//----------

let arr = [1,2,3];
let t = Math.max(...arr);       // Math.max(arr[0],arr[1],arr[2])

let str = "Hello";
r =
[
   [...str],               // H,e,l,l,o
   // eq:
   Array.from(str),
];

